library(rix)

rix(
    r_ver = "latest",
    r_pkgs = c("ggplot2"),
    system_pkgs = NULL,
    git_pkgs = NULL,
    ide = "code",
    shell_hook = NULL,
    project_path = "~/Desktop/rix-mytutorial/",
    overwrite = TRUE,
    print = TRUE
)
